
SET(RW_ENABLE_ASSERT ${RW_ENABLE_ASSERT} )

# Enable the use of the default RW_CXX_FLAGS. These will be appended to CMAKE_CXX_FLAGS
SET(RW_ENABLE_CXX_FLAGS ${RW_ENABLE_CXX_FLAGS} )

# Enable building tests, examples and sandbox
SET(RW_BUILD_TESTS ${RW_BUILD_TESTS} )
SET(RW_BUILD_EXAMPLES ${RW_BUILD_EXAMPLES} )
SET(RW_BUILD_SANDBOX ${RW_BUILD_SANDBOX} )

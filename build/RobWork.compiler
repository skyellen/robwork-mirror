
# Extra compiler flags and settings for:

if (CMAKE_COMPILER_IS_GNUCXX)

  if (DEFINED MINGW)
    set(RW_CXX_FLAGS "-Wall -fmessage-length=0")
  else ()
    set(RW_CXX_FLAGS "-Wall -fPIC -fmessage-length=0")
    #set(RW_CXX_FLAGS "-Wall -fmessage-length=0")
  endif ()

elseif (DEFINED MSVC)

  # Compiler flags
  set(RW_CXX_FLAGS "")

  # This should be harmless, and is needed to compile things.
  add_definitions(-DNOMINMAX)

  # Without this Boost define for Visual Studio we can't link with lapack.
  add_definitions(-DBIND_FORTRAN_LOWERCASE_UNDERSCORE)

else ()

  # Other compilers:
  set(RW_CXX_FLAGS "")

endif ()
